# 신호 API 검증 보고서 (2026-03-28)

## ✅ 테스트 완료 요약

| 항목 | 상태 | 결과 |
|------|------|------|
| 단위 테스트 (Mock 데이터) | ✅ | 10/10 통과 |
| 실제 API 테스트 (US) | ✅ | 필드 검증 완료 |
| 실제 API 테스트 (KR) | ✅ | 필드 검증 완료 |
| Top API 테스트 | ✅ | limit 파라미터 정상 |
| AI 데이터 진단 | ✅ | 22행 저장, 신뢰도 분석 완료 |

---

## 🧪 테스트 1: 단위 테스트 (Mock 데이터)

**파일:** `scripts/test_signal_logic_fixed.js`

### 결과: 10/10 통과 ✅

```
✅ Test 1: 고신뢰도 매수 (유지)
   Input: score=75, confidence=0.75 → Result: 매수 [🚀 강한 매수]

✅ Test 2: 저신뢰도 매수 (다운그레이드)
   Input: score=65, confidence=0.30 → Result: 관망 [📈 매수]

✅ Test 3: 중신뢰도 매도 (다운그레이드)
   Input: score=25, confidence=0.45 → Result: 관망 [📉 매도]

✅ Test 4: 고신뢰도 매도 (유지)
   Input: score=15, confidence=0.75 → Result: 매도 [🔴 강한 매도]

✅ Test 5: AI 데이터 없음
   Input: score=73, confidence=null → Result: 매수 [📈 매수]

✅ Test 6: 관망 신호
   Input: score=50, confidence=0.50 → Result: 관망 [➡️ 관망]

✅ Test 7: Confidence 경계값 0.60
   Input: score=70, confidence=0.60 → Result: 매수 [📈 매수] (유지)

✅ Test 8: Confidence 0.59
   Input: score=30, confidence=0.59 → Result: 관망 [📉 매도] (다운)

✅ Test 9: 극단적 고신뢰도 매수
   Input: score=88, confidence=0.95 → Result: 매수 [🚀 강한 매수]

✅ Test 10: 극단적 저신뢰도 매도
   Input: score=5, confidence=0.05 → Result: 관망 [🔴 강한 매도]
```

### Confidence Threshold 로직

**규칙 (정규화됨)**
```javascript
// 신뢰도 >= 0.60: 매수/매도 신호 유지
// 신뢰도 < 0.60: 관망으로 다운그레이드
// 신뢰도 = null: 기술적 스코어만 사용
```

**경계값 처리**
- confidence = 0.60: 유지 (신뢰도 충분)
- confidence = 0.59: 다운 (신뢰도 부족)

---

## 🧪 테스트 2: 실제 API (US 시장)

**파일:** `scripts/test_signals_api.js http://localhost:3006 us`

### 결과: 모든 필드 검증 완료 ✅

#### 필드 검증
```
✅ ticker 필드
✅ name 필드
✅ score (0-100)
✅ techScore (0-100)
✅ aiScore (0-100 or null)
✅ confidence (0-1 or null)
✅ action ('매수' / '매도' / '관망')
✅ signalStrength (5분류)
✅ hasAI (boolean)
```

#### 점수 분포 (7개 종목)
```
최소: 48, 최대: 57, 평균: 54.29
→ 모두 관망 영역 (40-60)
```

#### SignalStrength 분류

```
🚀 강한 매수      0개 (0%)
📈 매수         0개 (0%)
➡️ 관망        7개 (100%) ← 모든 종목
📉 매도         0개 (0%)
🔴 강한 매도      0개 (0%)
```

**원인:** confidence 최대값 = 0.40 (< 0.60 threshold)

#### Top API 검증

```
✅ limit=1   → 1개 반환
✅ limit=5   → 5개 반환
✅ limit=50  → 7개 반환 (전체)
✅ limit=0   → 1개 반환 (보정)
✅ limit=100 → 7개 반환 (보정)
✅ 점수 기준 내림차순 정렬 ✅
```

---

## 🧪 테스트 3: AI 데이터 진단

**파일:** `scripts/diagnose_ai_data.py`

### AI 예측 데이터 현황

| 항목 | 값 | 평가 |
|------|-----|------|
| 총 행 수 | 22행 | ✅ 충분 |
| 신뢰도 범위 | 0.0923 ~ 0.5716 | ⚠️ 낮음 |
| 신뢰도 평균 | 0.3586 | ⚠️ 낮음 |
| 신뢰도 >= 0.60 | 0개 (0%) | ❌ 매우 낮음 |
| 신뢰도 0.40-0.60 | 9개 (41%) | ✅ 적정 |
| 신뢰도 < 0.40 | 13개 (59%) | ⚠️ 과다 |

### 신호 레이블 분포

```
HOLD       12개 (54.5%) ██████████
SELL        8개 (36.4%) ███████
BUY         2개 (9.1%)  █
```

### 종목별 데이터

```
주요 종목별 버퀘이(상위 4개):
- US7개 종목: 각 2행씩 (QQQ, SPY, GLD, TQQQ, IWM, SOXL, TLT)
- KR8개 종목: 각 1행
```

---

## 📊 현재 상황 분석

### ⚠️ 발견된 이슈

**Issue 1: Model Confidence 과도하게 낮음**
- 신뢰도 최대값 0.5716 (< 0.60)
- 모든 신호가 confidence threshold에 의해 필터링됨
- 결과: 모든 신호 → '관망' (신호 가치 저하)

**원인 추정:**
1. 모델의 calibration이 과도하게 보수적
2. 클래스 불균형 (BUY 9%, HOLD 54.5%)
3. 학습 템플릿이나 정규화 이슈

### ✅ 개선 방안

#### 1단계: 모델 재훈련 (신뢰도 개선)
```bash
# 고급 튜닝 (100 trials)
python scripts/train_lgbm.py --tune --market us --n-trials 100
```

**목표:**
- confidence >= 0.60인 데이터 30% 이상 확보
- HOLD 과다 문제 해결

#### 2단계: 대안 (Threshold 동적 조정) - 필요시
```
현재: 고정 0.60 threshold
개선: percentile 기반 (상위 30% = 0.신뢰도 자동)
```

---

## 📋 코드 수정 사항

### 수정 1: Confidence 필터링 로직 정규화

**파일:** `app/api/signals/route.ts` + `app/api/signals/top/route.ts`

**이전:**
```typescript
if (action === '매도' && lgbm_prob > CONFIDENCE_THRESHOLD_SELL) {
  return '관망'  // 비대칭 (매도와 매수가 다른 기준)
}
```

**수정 후:**
```typescript
if ((action === '매수' || action === '매도') && lgbm_prob < 0.60) {
  return '관망'  // 대칭 (동일한 기준)
}
```

**개선점:**
✅ 매수/매도 기준 통일
✅ 신뢰도 >= 0.60일 때만 강한 신호 유지
✅ 경계값 정확도 향상

---

## 🎯 다음 단계

### 우선순위 1: 모델 신뢰도 개선
```
목표: confidence 분포 개선
실행: python scripts/train_lgbm.py --tune --n-trials 100
예상 효과: confidence >= 0.60 데이터 30% 이상 확보
```

### 우선순위 2: 피처 추가
- return_2d, return_3d (2일, 3일 수익률)
- gap_pct (시가대비 변화)

### 우선순위 3: UI 업데이트
- signalStrength 시각화
- confidence 신뢰도 표시
- 아이콘 설정

---

## 📈 성능 지표 추적

| 지표 | 2026-03-28 | 목표 | 상태 |
|------|-----------|------|------|
| API 응답 필드 완성도 | 100% | 100% ✅ | ✅ |
| 유닛 테스트 통과율 | 100% | 100% ✅ | ✅ |
| Top API 기능성 | 100% | 100% ✅ | ✅ |
| 신뢰도 >= 0.60 비율 | 0% | 30%+ | ❌ 개선 필요 |
| 활성 신호 비율 | 0% | 50%+ | ❌ 개선 필요 |

---

**검증 완료 시각:** 2026-03-28 오후
**다음 검증 예정일:** 모델 재훈련 후
