# 2026-03-31 개발일지

## 요약
VSCode Chat 작업 중 발생한 누적 버그 수정, KR 모델 정상화, US 개별주 확장, 자동 재학습 인프라 구축.

---

## 수정된 버그

### 1. `_LEVERAGE3X_PATTERNS` NameError
- **파일**: `scripts/train_lgbm.py`
- **문제**: `_get_label_threshold()` 함수에서 참조하는 4개 패턴 변수가 정의되지 않음
- **원인**: VSCode Chat 작업 중 변수 정의 누락
- **수정**: `_TIER_THRESHOLDS` 아래에 패턴 집합 추가
  ```python
  _LEVERAGE3X_PATTERNS = {'SOXL', 'TQQQ', ...}
  _LEVERAGE2X_PATTERNS = {'QLD', 'SSO', '122630', ...}
  _HIGH_VOL_PATTERNS   = {'005930', '000660', ...}
  _BOND_INVERSE_PATTERNS = {'TLT', 'TBT', '114800', ...}
  ```

### 2. `ai_predictions` ON CONFLICT 오류 (42P10)
- **문제**: `add_model_version_to_predictions.sql` 마이그레이션으로 UNIQUE 제약이 `(ticker, date, model_version)`으로 변경됐으나 Python 코드는 `ticker,date`만 사용
- **수정**:
  - `on_conflict="ticker,date,model_version"` 으로 변경
  - `model_version: "A"` 필드를 upsert rows에 추가
  - Supabase SQL Editor에서 마이그레이션 수동 실행

### 3. `collect_yfinance.py` 다중 버그
- **`import os` 누락**: `NameError: name 'os' is not defined`
- **`.env.local` 미로드**: Supabase 환경변수 인식 불가 → fallback 사용
- **KR 심볼 형식 불일치**: DB에 `005930`(6자리)로 저장된 심볼을 yfinance는 `005930.KS` 형식으로 요구
- **수정**: `import os` 추가, `.env.local` 로더 추가, `_normalize_kr_symbol()` 함수 추가

### 4. `upsert_supabase.py` UTF-8 BOM 오류
- **문제**: PowerShell `Out-File -Encoding utf8`이 UTF-8 BOM(utf-8-sig) 형식으로 저장 → JSON 파싱 실패
- **수정**: `read_text(encoding="utf-8-sig")`로 변경

---

## 기능 개선

### 5. KIS Cron ETF + 외국인 순매수 추가
- **파일**: `app/api/cron/kis/route.ts`
- **추가 내용**:
  - KR ETF 4개 수집 (KODEX200, KOSDAQ150, 반도체, 인버스2X)
  - `getKisInvestorFlow()` / `getKisEtfInvestorFlow()` 호출로 외국인 순매수 → `foreign_net_flow` 저장
  - 중복 코드 → `processOhlcv()` 공통 함수로 통합

### 6. LGBM Confidence 임계값 조정
- **파일**: `.env.local`
- **변경**: `BUY 0.55 → 0.42`, `SELL 0.60 → 0.38`
- **이유**: 3분류 모델에서 확률 최대값이 ~0.57 수준이라 0.60 기준은 신호가 전혀 안 나오는 구조

### 7. GitHub Actions 월간 자동 재학습
- **파일**: `.github/workflows/lgbm-predict.yml`
- **추가**: `cron: "0 2 1 * *"` (매월 1일 02:00 UTC) — Optuna 튜닝 포함 전체 재학습
- **기존**: 평일 매일 predict-only 유지

### 8. 학습 데이터 3년 확장
- **파일**: `scripts/train_lgbm.py`
- **추가**: `TRAIN_LOOKBACK_YEARS = 3` 상수 + `load_daily_indicators()`에 `gte(cutoff)` 날짜 필터
- **목적**: 더 많은 시장 사이클 학습으로 정확도 향상

---

## 학습 결과

### KR 모델 (재학습 완료)
| 항목 | 값 |
|------|---|
| CV Accuracy | 57.76% |
| 분류 방식 | 이진 (1일 후 상승/하락) |
| 예측 종목 | 64개 |
| DB 저장 | ai_predictions 66건, signal_history 66건 |
| 신호 상태 | ✅ BUY/SELL 신호 정상 생성 (up=0.53~0.58) |

### US 모델 (개별주 포함 재학습)
| 항목 | 값 |
|------|---|
| CV Accuracy | 40.81% (균형 ~33%) |
| 학습 종목 | 7개 ETF → **55개 (NVDA, AAPL, MSFT 등 개별주 포함)** |
| 예측 종목 | 55개 |
| DB 저장 | ai_predictions 55건, signal_history 55건 |
| 신호 상태 | ⚠️ 전부 HOLD (buy≈0.30~0.35 — 임계값 0.42 미달) |

### US 모델 과제
- 현재 balanced_accuracy ≈ 0.337 ≈ 랜덤(0.333) — 모델이 클래스를 구분 못하는 수준
- **원인 추정**: 1년치 데이터만 있어 학습 샘플 부족, 특히 개별주는 종목별 특성 차이가 큼
- **해결 방향**: 3년치 데이터 수집 후 재학습 → 다음 단계로 예정

---

## 데이터 수집 현황

### 365일치 수집 완료
- `collect_yfinance.py 365d` → `upsert_supabase.py` 파이프라인으로 DB 저장
- 총 122개 종목 (US ETF/개별주 55 + KR 주식/ETF 67)

### 3년치 수집 예정 (진행 중)
- `collect_yfinance.py 1095d` 실행 예정
- US 모델 재학습 정확도 개선 기대

---

## 자동화 구조 (완성)

| 주기 | 작업 | 플랫폼 |
|------|------|--------|
| 매일 평일 07:00 KST | KR OHLCV + 지표 수집 | Vercel cron |
| 매일 평일 07:30 KST | ECOS + actual_return 업데이트 | Vercel cron |
| 매일 01:00 UTC | FRED 거시지표 | Vercel cron |
| 매일 평일 22:00 UTC | LightGBM 예측 → DB | GitHub Actions |
| **매월 1일 02:00 UTC** | **전체 재학습 (Optuna 튜닝)** | **GitHub Actions (신규)** |

---

## 다음 단계
1. **3년치 데이터 수집 완료 후 US 모델 재학습** — 정확도 개선 핵심
2. **심볼 중복 정리** — `005930` vs `005930.KS` 같은 회사가 market_master에 이중 등록
3. **FutureWarning 해소** — `pct_change(fill_method=None)` 적용 (minor)
