# 개발일지 2026-03-26

- 작성 시각: 2026-03-26
- 작성자: SSOMMII

## 1) 오늘 완료된 작업

### 데이터 수집기 구현 (lib/collectors/)
- `lib/collectors/kr/kis.ts` — 한국투자증권 KIS Developers API (OAuth2 토큰 캐싱, 시세/수급/일봉)
- `lib/collectors/kr/ecos.ts` — 한국은행 ECOS API (CPI, 기준금리, 원달러 환율)
- `lib/collectors/kr/naverNews.ts` — 네이버 금융 뉴스 크롤러 (cheerio 기반 헤드라인/종목별 뉴스)
- `lib/collectors/us/finnhub.ts` — Finnhub API (Quote, Sentiment, 시장뉴스, 캔들)
- `lib/collectors/us/apeWisdom.ts` — ApeWisdom API (Reddit 종목 언급량 순위, API Key 불필요)
- `lib/collectors/common/fearGreed.ts` — Alternative.me 공포/탐욕 지수 (API Key 불필요)

### Python 수집 스크립트 구현 (scripts/)
- `scripts/collect_yfinance.py` — yfinance 기반 미국 ETF/선물/환율/원자재 OHLCV 수집
- `scripts/collect_fred.py` — FRED 거시경제 지표 수집 (연방금리, 실업률, GDP, CPI, 국채금리, 환율)
- `scripts/requirements.txt` — Python 의존성 (yfinance, pandas, pandas_datareader)

### 기타
- `package.json` — cheerio, @supabase/supabase-js 추가
- `.env.example` — KIS, ECOS, Finnhub, FRED API 키 항목 추가

---

### Supabase DB 설계 및 실데이터 연동 (2026-03-26 오후)

#### DB 초기화
- `db/schema.sql` — 11개 테이블 설계 (market_master, daily_indicators, ai_signals, global_indicators, foreign_flow, signal_weights, etf_mapping, signal_history, sentiment_cache, ai_predictions, users)
- `db/supabase-init.sql` 의 `\i schema.sql` 오류 확인 → psql 전용 명령어로 Supabase 웹 에디터 미지원
- `db/init_combined.sql` 생성 → extensions + 전체 테이블 + 샘플 데이터를 하나로 합쳐 Supabase SQL Editor에서 실행 가능하도록
- Supabase SQL Editor에서 `init_combined.sql` 실행 완료 → 11개 테이블 생성 확인

#### 권한 설정
- `GRANT ALL ON ALL TABLES IN SCHEMA public TO service_role` — Python upsert용
- `GRANT SELECT ON ALL TABLES IN SCHEMA public TO anon, authenticated` — 프론트엔드 읽기용

#### Python upsert 파이프라인
- `scripts/upsert_supabase.py` — `.env.local` 자동 로드 코드 추가 (Path 기반)
- `maybe_single()` 반환값 null 처리 버그 수정 (`res` → `res and res.data` 체크)
- `py scripts/upsert_supabase.py --mode snapshot` 실행 → `global_indicators` 실데이터 upsert 완료
- `py scripts/collect_yfinance.py 30d | py scripts/upsert_supabase.py --mode yfinance` 실행
  - `market_master` 20개 종목 자동 등록 (QQQ, SPY, ^GSPC, ^KS11 등)
  - `daily_indicators` 573건 upsert 완료

#### Next.js 실데이터 연동
- `lib/supabaseClient.ts` — `getDailyIndicatorsBySymbol`, `getLatestGlobalIndicatorsHistory` 함수 추가
- `app/api/global-indicators/route.ts` 신규 생성
  - 현재 절대값: `global_indicators` 최신 스냅샷
  - 일별 등락률: `daily_indicators` 30일치 데이터에서 최근 2일 종가 비교 (^GSPC, ^IXIC, ^VIX, CL=F, GC=F, USDKRW=X)
- `app/api/market-summary/route.ts` 신규 생성 — symbol 기반 daily_indicators 조회
- `app/page.tsx` 수정
  - `useEffect` + `useState` 추가 → Supabase 실데이터 fetch
  - 글로벌 마켓 스트립 → 실데이터 반영
  - 인덱스 차트 (index1) → 실 OHLCV + 스파크라인 반영
  - 기술적 지표 → RSI/MACD null 시 mock fallback
  - LIVE 뱃지 + 마지막 업데이트 타임스탬프 표시

---

### 1단계 — 기술적 지표 + Index2 실데이터 연동 (2026-03-26 오후)

#### collect_yfinance.py 개선
- `pandas_ta` Python 3.14 numba 미지원 → `ta` 라이브러리(`pip install ta`)로 대체
- `^KQ11` (KOSDAQ) TARGETS에 추가
- `_compute_indicators()` 함수 추가 — RSI(14), MACD(12,26,9), SMA(50/120/200), 볼린저밴드(20,2), 스토캐스틱(14,3) 계산
- 지표 계산 시 내부적으로 항상 1년치 데이터 사용 → SMA200 정확도 확보, 요청 기간만 반환
- `_row_to_record()` 헬퍼로 지표 컬럼 포함한 레코드 생성

#### upsert_supabase.py 개선
- `daily_indicators` upsert 시 rsi, macd, signal_line, sma_50/120/200, bollinger_upper/middle/lower, stoch_k/d 컬럼 포함
- `py scripts/collect_yfinance.py 30d | py scripts/upsert_supabase.py --mode yfinance` 실행
  - `^KQ11` market_master id=29 신규 등록
  - 630건 upsert 완료 (지표값 포함)

#### page.tsx — index2 실데이터 연결
- `index2Rows` state 추가
- `useEffect` — KR: `^KQ11`, US: `^IXIC` fetch
- `realIndex2` 빌드 — OHLCV + 스파크라인 (mock fallback 포함)
- 렌더링: `realIndex2 ?? d.index2`

#### tailwind.config.js 버그 수정
- `content`에 `./lib/**/*.{ts,tsx}` 추가
- 원인: `detailData.ts`, `marketData.ts`에 정의된 gradient 클래스(`from-indigo-600 via-indigo-700 to-purple-700` 등)가 빌드에서 누락되어 상세분석 페이지 배너 색상 消失

---

### 2단계 — Today's Signal 실데이터 연동 (2026-03-26 저녁)

#### /api/signals 신규 라우트
- `app/api/signals/route.ts` 생성
- 대상 ETF: QQQ, SPY, SOXL, TQQQ, IWM, GLD, TLT
- market_master → daily_indicators 최근 2일치 조회 (2쿼리)
- AI Score 계산 로직 (범위 -4.5 ~ +4.5):
  - RSI < 30 → +1.5 / < 45 → +0.5 / > 70 → -1.5 / > 55 → -0.5
  - MACD > Signal Line → +1.0 / else → -1.0
  - Close > SMA50 → +0.5 / else → -0.5
  - Close > SMA200 → +0.5 / else → -0.5
  - Stoch K < 20 → +1.0 / > 80 → -1.0
- Score ≥ 1.5 → 매수 / ≤ -1.0 → 매도 / else → 관망
- AI Score 내림차순 정렬

#### page.tsx — Today's Signal 연결
- `realSignals` state 추가
- `useEffect` — `/api/signals` fetch
- 렌더링: `realSignals ?? d.signals`

---

## 2) 현재 실데이터/목업 현황

| 섹션 | 상태 | 비고 |
|------|------|------|
| 글로벌 마켓 스트립 | ✅ 실데이터 | global_indicators + daily_indicators |
| 인덱스 차트 index1 | ✅ 실데이터 | OHLCV + 스파크라인 (^KS11 / ^GSPC) |
| 상태바 strip preview | ✅ 실데이터 | global_indicators |
| 기술적 지표 (RSI/MACD 등) | ✅ 실데이터 | ta 라이브러리 기반 계산, daily_indicators upsert |
| 인덱스 차트 index2 | ✅ 실데이터 | ^KQ11 (KOSDAQ) / ^IXIC (NASDAQ) |
| Today's Signal | ✅ 실데이터 | /api/signals, 지표 기반 AI Score |
| Insight 카드 | ❌ 목업 | OpenAI API 키 필요 |
| 포트폴리오 | ❌ 목업 | 사용자 인증 필요 |

---

## 3) 다음 단계 계획

### 3단계 — Insight 카드 (OpenAI API 키 필요)
- OpenAI API 연동 → 글로벌 지표 + 기술적 지표 요약 → 한 줄 인사이트 생성
- `/api/insight` 라우트 추가

### 4단계 — 포트폴리오 (Supabase Auth)
- Supabase Auth 설정 (별도 키 불필요, 기존 프로젝트에서 설정 가능)
- 사용자별 포트폴리오 저장/조회

---

## 4) 미해결/보류 항목

- 각 수집기를 호출하는 Next.js API route 미구현 (`app/api/collect/...`)
- 수집 스케줄러 (cron) 미설정
- KIS, ECOS, Finnhub API Key 미발급
- Insight 카드: OpenAI API Key 미발급
