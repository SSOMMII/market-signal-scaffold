# 개발일지 2026-03-25

- 작성 시각: 2026-03-25 14:10
- 작성자: GitHub Copilot

## 1) 오늘 해결하고자 한 문제

- 기존 Next.js 프로젝트에서 중복된 컴포넌트/데이터가 섞여 있는 구조를 정리
- 동일한 SVG 아이콘/차트 코드가 여러 페이지에 중복되어 유지보수가 어려움
- 데이터가 페이지 내부에 하드코딩돼 있어 동적 처리/API 연동이 어려움
- lint/의존성 충돌 확인

## 2) 해결된 항목

- `components/icons.tsx`로 SVG 아이콘 공통 컴포넌트 모듈화
- `components/MiniChart.tsx`로 sparkline 재사용 컴포넌트 분리
- `lib/detailData.ts`, `lib/historyData.ts`, `lib/marketData.ts`로 데이터 구조 분리
- `app/detail/page.tsx`, `app/history/page.tsx`, `app/page.tsx`에서 공통 모듈 import 적용
- `PRD_Global_Market_Bridge_AI_v1.0.md` 생성

## 3) 미해결/보류된 항목

- `next lint` 실행 도중 `eslint` 버전 충돌(8 vs next@16 기준 9 이상)으로 lint 검사 완료하지 못함
- `lib/marketData.ts`에 실제 data 이식 작업이 placeholder 상태 (추후 채움 필요)
- API/동적 Service 계층(백엔드 연동) 미구현

## 4) 향후 개발을 위한 컨텍스트

- 다음 작업: `lib/marketData.ts`에 실제 `krData`/`usData` 채우기, 페이지에서 `useMarket`에 따라 데이터 로드
- 이후 API 스텁 추가: `services/marketService.ts` 생성, `getMarketData()`/`getHistoryData()` 함수 작성
- 통합 테스트: `next dev` 기본 동작 확인, `/`, `/detail`, `/history` 랜더링 검증
- CI 준비: `npm install --legacy-peer-deps` 또는 `eslint@^9`으로 패키지 잠금 개선

---

## 5) 규칙 추가: Claude Project Rules

- 이 저장소에서는 개발일지를 `개발일지/YYYY-MM-DD.md` 형식으로 생성
- 각 일지에 작성 시각, 해결 문제, 완료 항목, 미완료 항목, 향후 컨텍스트를 포함
- 모든 개발 작업은 PRD와 연동하여 기록
- `PRD_Global_Market_Bridge_AI_v1.0.md` 기반으로 기능 확장 관리


## 6) 2026-03-25 추가 기록 (GitHub Copilot)
- `.gitignore`에 `.next/`, `node_modules/`, `.env*` 등 빌드/환경 항목 추가
- `npm install --save-dev prettier eslint ...`로 코드 품질 도구 설치
- `.prettierrc`, `.eslintrc.json`, `.prettierignore`, `.vscode/settings.json` 추가
- `API_SPEC.md`, `.env.example` 생성
- `app/api/market-masters/route.ts`, `app/api/daily-indicators/route.ts` API 라우트 구현
- `supabaseClient.ts` 및 내부 API 함수 `getMarketMasters`, `getDailyIndicators` 확정
- `next dev` Fast Refresh 로그 정상 작동(1159ms → 692ms → 205ms) 확인
- `npm` 커밋/푸시 완료

## 7) 2026-03-26 계획 (내일 할 일: 데이터 소스/크롤링/DB/출력)
1. 데이터 소스 및 우선순위 확정
   - 거시경제:
     - Bloomberg / Reuters (KRX/국제경제 지표)
   - 분석:
     - Seeking Alpha (종목/섹터 리포트, 뉴스)
   - 심리:
     - Reddit(Investing, r/WallStreetBets), Toss(국내 커뮤니티 태그)

2. 크롤링 + 수집 구조 설계
   - 서버: `python` + `scrapy` / `requests` + `BeautifulSoup` / `playwright`
   - API 매칭:
     - `GET /api/crawl/bloomberg/market` => 매 시각 KRX/US 주요 지수
     - `GET /api/crawl/reuters/economic` => CPI, GDP, 금리, 환율
     - `GET /api/crawl/seekingalpha/symbol/{ticker}` => 실적/목표주가/애널리스트 의견
     - `GET /api/crawl/reddit/sentiment` => 키워드 기반 코멘트 + 패턴
     - `GET /api/crawl/toss/sentiment` => 재테크 게시판 트렌드

3. DB 설계 (Supabase/Postgres)
   - 테이블: `economic_indicators` (`date, type, value, source`)
   - 테이블: `news_analysis` (`source, category, title, summary, sentiment_score, published_at`)
   - 테이블: `sentiment_scores` (`platform, target, positive, negative, neutral, score, collected_at`)
   - 테이블: `etf_signals` (`etf_code, date, signal, signal_score, detail`)
   - 테이블: `one_line_summary` (`date, category, summary_text`)

4. Python 크롤링 스크립트 초안
   - `crawler/bloomberg.py`:
     - 함수 `fetch_bloomberg_macro()` => `economic_indicators` insert/update
   - `crawler/seeking_alpha.py`:
     - 함수 `fetch_seekingalpha_ticker(ticker)` => `news_analysis` insert/update
   - `crawler/reddit.py`:
     - 함수 `fetch_reddit_sentiment(terms)` => `sentiment_scores` insert/update
   - `crawler/toss.py`:
     - 함수 `fetch_toss_sentiment()` => `sentiment_scores` (국내)

5. 출력/모델 통합 (Next.js API)
   - `app/api/forecast/v2/etf-signal`:
     - 입력: `date`, `market` (KRX/US), `etf_code`
     - 출력: `signal`, `probability`, `key_drivers`
   - `app/api/forecast/v2/summary`:
     - 입력: `date`
     - 출력: `one_line_summary`, `overall_sentiment`
   - `app/api/forecast/v2/sentiment`:
     - 입력: `source` (reddit/toss/seekingalpha)
     - 출력: `score`, `trend`

6. 개발 우선순위
   - A. 크롤링 및 DB 스케줄 (cron/Cloud Functions) 구성
   - B. DB 정규화 및 인덱스 추가 (`collected_at`, `date`)
   - C. API 라우터 작성 + 캐시(ttl)
   - D. 프론트: `ETF 신호 >= 70` 하이라이트, `한줄요약`, `sentiment 점수` 표시
   - E. E2E 검증: 실제 1일치 텀샘플 라우팅(데이터 수집→API→UI)
   - F. 외국인수급 연속성 로직 통합
   - G. 심리반전 조건 (커뮤니티 긍정↑ + VIX↑) 메시지 템플릿
   - H. 출력 한줄 설계 반영: 입력/처리/출력 제품화

