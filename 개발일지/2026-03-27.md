# 개발일지 2026-03-27

## 오늘 완료한 작업

### 1. Supabase Auth 기반 인증 시스템 완성

**신규 파일**
- `context/AuthContext.tsx` — Supabase Auth 세션 전역 관리 (useAuth 훅 제공)
- `components/Toast.tsx` — 성공/에러 토스트 알림 컴포넌트 (useToast 훅)
- `components/ProtectedPage.tsx` — 로그인 필요 페이지용 래퍼 컴포넌트
- `app/signup/page.tsx` — 회원가입 페이지
- `app/verify-email/page.tsx` — 이메일 인증 안내 페이지

**수정 파일**
- `app/login/page.tsx` — 기존 목업 → Supabase Auth 실연동
  - 로그인 / 회원가입 / 비밀번호 찾기 3가지 모드 통합
  - `signInWithPassword` / `signUp` / `resetPasswordForEmail` 연동
  - URL 파라미터로 `?mode=signup`, `?next=/dashboard` 지원
- `components/Nav.tsx` — LoginModal 제거 → `useAuth` + `useToast` 기반으로 교체
  - 로그아웃 시 `supabase.auth.signOut()` 실행 + 토스트 알림
- `app/layout.tsx` — `AuthProvider`, `ToastProvider` 전역 주입

### 2. KIS API 크론 고도화 (120일치 OHLCV → 기술적 지표 계산)

**수정 파일: `app/api/cron/kis/route.ts`**
- 기존: 당일 시세 1건만 수집
- 변경: 180일치 일봉 OHLCV 수집 → TypeScript로 지표 직접 계산 후 upsert
  - `getKisDailyOhlcv(symbol, startDate, endDate)` 신규 함수 활용
  - `lib/indicators.ts`의 `calcRSI` / `calcMACD` / `calcSMA` / `calcBollinger` / `calcStochastic` 호출
  - 최신 날짜 1건만 `daily_indicators` 테이블에 upsert

**신규 파일: `lib/indicators.ts`**
- RSI(14), MACD(12/26/9), SMA(20/50/200), 볼린저밴드(20일), 스토캐스틱(14/3) 순수 TypeScript 구현

### 3. FRED API 크론 신규 추가

**신규 파일: `app/api/cron/fred/route.ts`**
- FRED API에서 USD/KRW 환율 + 10년/2년 국채금리 + 장단기금리차 수집
- `global_indicators` 테이블 upsert
- 스케줄: 매일 01:00 UTC (`vercel.json` 등록 완료)

### 4. Signals API KR/US 분리

**수정 파일: `app/api/signals/route.ts`**
- 기존: US ETF만 반환
- 변경: `?market=kr` / `?market=us` 쿼리 파라미터로 분리
  - KR: 국내 주식 5종 (삼성전자/SK하이닉스/NAVER/카카오/현대차)
  - US: ETF 7종 (QQQ/SPY/SOXL/ARKK/GLD/DIA/TLT)

---

## 현재 실데이터 현황

| 섹션 | 상태 |
|------|------|
| 글로벌 마켓 스트립 | ✅ 실데이터 |
| 인덱스 차트 | ✅ 실데이터 |
| 기술적 지표 (RSI/MACD 등) | ✅ 실데이터 |
| Today's Signal (KR/US) | ✅ 실데이터 |
| 인증 (로그인/회원가입) | ✅ 실연동 완료 |
| Insight 카드 | ❌ 목업 (OpenAI 연동 예정) |
| 포트폴리오 | ❌ 목업 (인증 완료 후 개발) |

## 크론 스케줄 현황

| 크론 | 스케줄 | 상태 |
|------|--------|------|
| fear-greed | 매일 00:05 UTC | ✅ |
| ape-wisdom | 매일 00:10 UTC | ✅ |
| finnhub | 매일 00:20 UTC | ⚠️ 유료 제한 |
| kis | 평일 07:00 UTC | ✅ 180일치 OHLCV + 지표 계산 |
| ecos | 평일 07:30 UTC | ✅ |
| fred | 매일 01:00 UTC | ✅ 신규 추가 |

---

## 다음 단계 (3단계)

1. **OpenAI API 연동** — Insight 카드 실데이터화
2. **뉴스 수집 → 감성분석** — Finnhub 크론을 뉴스 수집 + OpenAI 감성분석으로 교체
3. **포트폴리오** — 인증 완료 기반 개인 포트폴리오 기능
