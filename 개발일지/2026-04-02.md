# 2026-04-02 개발일지

## 요약

빌드 안정화 작업: ESLint 순환참조 에러 해결, Migration API `exec_sql` RPC 교체, Geo-Risk에 OpenAI gpt-4o-mini 연동 구현.

---

## 1. 빌드 에러 수정

### 1) ESLint Circular JSON 에러 해결

- **파일**: `.eslintrc.json`, `package.json`
- **증상**: `npm run lint` 및 `npm run build` 시 `Converting circular structure to JSON` 에러 발생, `react` 플러그인 순환참조로 빌드 중단.
- **원인**: `eslint-config-next@16.2.1` (Next.js 16용, ESLint 9 flat config 전용)이 `next@14.2.35` 와 버전 불일치. 두 config 포맷이 충돌하며 플러그인 객체 순환참조 발생.
- **수정**:
  - `eslint-config-next` → `14.2.35`로 다운그레이드 (next 버전과 일치)
  - `.eslintrc.json`에서 중복 선언된 `plugin:react/recommended`, `plugin:react-hooks/recommended` 제거
  - `@typescript-eslint/recommended` + parser/plugin만 유지
  - `caughtErrorsIgnorePattern: "^_"` 추가 (catch 변수 ignore 패턴)
- **결과**: `npm run lint` Error 0개 (Warning만 남음)

### 2) Migration API `exec_sql` RPC 에러 해결

- **파일**: `app/api/migrate/route.ts`
- **증상**: 빌드 시 `Migration error: { code: 'PGRST202' }` 로그 출력. Supabase에 `exec_sql` 함수 미등록으로 DDL 실행 불가.
- **수정**: `exec_sql` RPC 방식 완전 제거 → admin 클라이언트로 `calibration_method` 컬럼 SELECT 시도 후 존재 여부 판단
  - 컬럼 있음 → `200 { success: true, message: "Migration already applied" }`
  - 컬럼 없음 → `412 { success: false, sql: "..." }` (수동 실행용 SQL 포함 응답)
- **결과**: 빌드 중 Migration error 로그 완전 제거

### 3) `lib/abTest.ts` unused variable 에러 수정

- **파일**: `lib/abTest.ts`
- **수정**: `catch (e)` → `catch (_e)` (두 곳)
- **결과**: `@typescript-eslint/no-unused-vars` Error 제거

---

## 2. Geo-Risk OpenAI gpt-4o-mini 연동 구현

- **파일**: `app/api/geo-risk/route.ts`, `app/api/cron/geo-risk/route.ts`
- **이전**: 키워드 룰 기반(`applyGeoRules()`) 분석만 지원, TODO 주석으로 LLM 연동 예정 표시
- **구현**:
  - `OPENAI_API_KEY` 환경변수 설정 시 → gpt-4o-mini로 헤드라인 분석 (최대 80개)
  - API 호출 실패 시 → 기존 키워드 룰로 자동 fallback (안정성 보장)
  - 응답에 `analysisMethod: 'openai' | 'rules'` 필드 추가 (디버깅/모니터링용)
  - gpt-4o-mini 프롬프트: `GeoSignal[]` JSON 스키마 강제, temperature 0.1, max_tokens 1500
- **비용 관리**: `revalidate = 3600` (1시간 Next.js 캐시) 유지 → API 호출 최소화

---

## 3. 최종 빌드 결과

```
✓ Compiled successfully
✓ Linting and checking validity of types (Error 0, Warning only)
✓ Generating static pages (37/37)
→ Migration error 로그 제거
→ ESLint 에러 제거
```

---

## 4. 현황 및 다음 단계

### 데이터 연동 현황 (2026-04-02 기준)

| 섹션 | 상태 | 비고 |
| :--- | :--- | :--- |
| 공모주 캘린더 | ✅ 실데이터 | 38.co.kr 크롤링 |
| 상세분석 — 배너·ETF·기술지표 | ✅ 실데이터 | API 실패 시 하드코딩 fallback |
| 상세분석 — 섹터 히트맵 | ✅ 실데이터 | `market_master.sector` 컬럼 채워져 있어야 동작 |
| 상세분석 — 일간/주간/섹터별/커스텀 탭 | ⚠️ UI만 있음 | 탭 클릭해도 화면 안 바뀜 (조건부 렌더링 미구현) |
| 상세분석 — AI 리포트 | ❌ 하드코딩 | `lib/detailData.ts` 고정 텍스트 |
| 예측이력 | ⚠️ 실데이터(데이터 부족) | `ai_predictions` 테이블 데이터 1건 — Python 스크립트 수동 실행 필요 |
| Geo-Risk | ✅ OpenAI gpt-4o-mini | 키워드 룰 fallback 포함 |

---

## 5. 점검 이슈 및 다음 할 일

### [이슈 1] 상세분석 탭 기능 미구현 (일간/주간/섹터별/커스텀)

- **현황**: `tabs` 배열과 `activeTab` state는 존재하나, 탭 값에 따른 조건부 렌더링 로직이 없음. 탭을 클릭해도 ETF 목록·지표·섹터·리포트 모든 섹션이 탭 관계없이 항상 표시됨.
- **할 일**:
  - 탭별로 보여줄 섹션을 정의 (일간: ETF+지표, 주간: 주간 추세, 섹터별: 히트맵 중심, 커스텀: 포트폴리오)
  - `activeTab` 기반 조건부 렌더링 추가

### [이슈 2] 상세분석 AI 리포트 하드코딩

- **현황**: `app/detail/page.tsx` 392-393줄 — `d.reportContent[reportTab].title/body` = `lib/detailData.ts` 고정 텍스트 100%
- **할 일** (OpenAI 연동 시):
  - `/api/analysis/[symbol]` 또는 별도 `/api/report` 엔드포인트에서 gpt-4o-mini로 리포트 생성
  - 탭별(일간/주간/섹터별/커스텀) 프롬프트 분리
  - 비용 절감: `revalidate` 또는 Supabase 캐싱으로 1일 1회 생성
  - **우선순위 낮음** (OpenAI 비용 발생 → 다른 작업 완료 후 마지막 적용)

### [이슈 3] 예측이력 자동 저장 크론 없음

- **현황**: `ai_predictions` 테이블에 데이터 1건뿐. Python LGBM 스크립트(`scripts/`)를 수동으로 1회 실행한 결과만 DB에 있음. 매일 자동 예측 저장 크론이 없어 이력이 쌓이지 않음.
- **구조**:
  - `app/api/history/route.ts` → `ai_predictions` 테이블에서 대표 종목(069500.KS / SPY) 최근 30건 조회
  - 데이터가 없으면 빈 배열 반환 → 예측이력 페이지 빈 화면
- **할 일**:
  1. Python LGBM 예측 스크립트를 매일 자동 실행하는 방법 결정
     - 옵션 A: Vercel Cron → `/api/cron/predict` 엔드포인트 신규 생성 (JS 포팅 필요)
     - 옵션 B: GitHub Actions에서 Python 스크립트 매일 실행 후 결과를 Supabase에 저장
     - 옵션 C: 로컬 crontab으로 매일 실행 (개발 단계용 임시)
  2. `ai_predictions` 에 충분한 데이터가 쌓이면 예측이력 페이지 자동 검증

### [이슈 4] `market_master.sector` 컬럼 데이터 확인 필요

- **현황**: 섹터 히트맵 API는 `market_master.sector`를 기준으로 집계하는데, DB에 `sector` 값이 없으면 히트맵이 비어있거나 하드코딩 fallback으로 표시됨.
- **할 일**: Supabase에서 `SELECT symbol, sector FROM market_master` 실행 후 sector 컬럼 채워져 있는지 확인. 없으면 각 심볼에 섹터 수동 입력.

### [이슈 5] Supabase 마이그레이션 수동 실행 필요

- **파일**: `db/migrations/add_calibration_method_to_predictions.sql`
- **할 일**: Supabase SQL Editor에서 해당 파일 내용 실행 (calibration_method 컬럼 추가)
